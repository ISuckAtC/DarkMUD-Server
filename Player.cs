using System;

namespace Player
{
    class PlayerClass
    {
        public string username, password;

        public PlayerClass(string username, string password)
        {
            this.username = username;
            this.password = password;
        }
    }
}